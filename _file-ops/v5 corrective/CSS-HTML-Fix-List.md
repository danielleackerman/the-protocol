# V5 CSS / HTML Fix List

## assets/css/protocol.css

- Adds semantic tokens: brand, danger, warning, success.
- Makes H1/H2/brand green and display-font based.
- Neutralizes button, label, row-term, and panel border color.
- Restores red only for `.callout` / breach contexts.
- Splits command sequence typography: mono labels, UI body text.
- Adds row formatting for `.level05-rule`, `.level06-step`, `.level06-rule`, `.level07-step`, `.level08-seal-row`, `.level09-step`, and `.level09-failure`.

## level pages

- Replaces `Explication` / `[Name] Explanation` pairs with `Reference` / `[Name]`.

## index.html

- Tightens lower reference-access copy to reduce editorial tone.
