# V5 Commit Punch List

1. Apply patch on `fix/v5-semantic-color-row-hierarchy`.
2. Check homepage: H1 green, command cards neutral, breach red only.
3. Check Level 02 command sequence: mono labels, readable body text.
4. Check Level 05 and Level 06 for collapsed rule/step rows.
5. Check every level page heading: no `Explication` + `Explanation` duplication.
6. Commit as `fix: apply semantic color and row hierarchy`.
