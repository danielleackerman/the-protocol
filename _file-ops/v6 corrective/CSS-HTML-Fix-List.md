# V6 CSS / HTML Fix List

## assets/css/protocol.css

- Added V6 semantic tokens for brand text, success, danger, warning, info, and neutral console lines.
- Set `.brand`, `.page-hero h1`, and `.daily-run__title h1` to neutral display color.
- Removed Space Age/display treatment from non-H1 headings and reference headings.
- Neutralized buttons, active navigation, row numbers, command verbs, utility chips, and level badges.
- Preserved green only for `ON`, READY/ACTIVE/COMPLETE statuses, and success state rows.
- Preserved red only for firewall/breach/danger panels and actions.
- Converted the Daily Run action grid into a linear console-row sequence.

## index.html

- Changed Reference Access copy so it reads as a functional reference module, not a second display title.

## levels/*.html

- Removed duplicate H2 page-title repeats inside Reference sections.
